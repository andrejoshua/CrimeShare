<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="@android:style/Theme.NoTitleBar.Fullscreen">
        <!-- Customize your theme here. -->
        <item name="android:background">@color/retroDark</item>
        <item name="android:colorBackground">@color/retroDark</item>
        <item name="android:windowBackground">@color/retroDark</item>
    </style>

</resources>